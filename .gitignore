# ---- 隐私文件：绝对不要提交 ----
# config.json 里装着学习通 Cookie 和你的推送 key，等同于账号凭证
config.json
state.json
state.json.tmp

# ---- 运行产物 ----
*.log
monitor.log
*.pid
panel.pid
tray.pid
monitor.pid
panel.show

# ---- Python ----
__pycache__/
*.pyc
*.pyo

# ---- 打包产物 ----
# exe 不进仓库：它挂在 GitHub Releases 上（几十 MB 塞进仓库会让每次 clone 都变慢）。
# cxmon.spec 要提交 —— 它是构建配方，别人靠它复现同样的 exe。
build/
dist/
